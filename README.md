# Weather App

A simple weather application built with Flask and OpenWeatherMap API.

## Features

- Search weather by city name
- Displays temperature (°C)
- Shows humidity and "feels like" temperature
- Clean, responsive UI
- Gradient background

## Setup

```bash
# Install dependencies
pip install -r requirements.txt

# Set your OpenWeatherMap API key
export OPENWEATHER_API_KEY="your-api-key"

# Run the app
python app.py
```

## Get API Key

1. Go to [OpenWeatherMap](https://openweathermap.org/api)
2. Sign up for a free account
3. Get your API key from the dashboard

## Usage

Open http://localhost:5000 in your browser.

Enter a city name and click "Search" to get the current weather.

## Built with GSD

This app was created using the GSD (Get Shit Done) framework:
- Structured project planning
- Phase-based development
- State management
